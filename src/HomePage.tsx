import React from "react";
import ReactDOM from "react-dom/client";
import Home from "./Home-alt";

ReactDOM.createRoot(document.getElementById("home-root") as HTMLElement).render(
  <React.StrictMode>
    <Home />
  </React.StrictMode>,
);
